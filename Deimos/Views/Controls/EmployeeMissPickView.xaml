<UserControl x:Class="Deimos.Views.Controls.EmployeeMissPickView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Deimos.ViewModels.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type vm:EmployeeMissPickVM}, IsDesignTimeCreatable=False}"
             d:DesignHeight="450" d:DesignWidth="800">
    <DockPanel>
        <StackPanel Orientation="Horizontal" DockPanel.Dock="Top">
            <Button Command="{Binding RefreshDataCommand}"
                    Width="30"
                    Height="30"
                    Margin="5"
                    ToolTip="Refresh">
                <Image Source="../../Resources/img/refresh.png"/>
            </Button>
        </StackPanel>

        <DataGrid></DataGrid>
    </DockPanel>
</UserControl>
