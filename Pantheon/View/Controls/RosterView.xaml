<UserControl x:Class="Pantheon.View.Controls.RosterView"
             x:Name="RosterControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:vm="clr-namespace:Pantheon.ViewModel.Controls"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance {x:Type vm:RosterVM}, IsDesignTimeCreatable=False}"
             d:DesignHeight="200" d:DesignWidth="200"
             Width="200">

    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <!-- Dark Theme -->
                <ResourceDictionary Source="../../Themes/Metro/Dark/MetroDark.MSControls.Core.Implicit.xaml"/>
                <ResourceDictionary Source="../../Themes/Metro/Dark/MetroDark.MSControls.Toolkit.Implicit.xaml"/>
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <Border BorderBrush="{StaticResource ForegroundBrush}"
            BorderThickness="1">
        <DockPanel>

            <!-- Roster Employee-Date Confirmation Text-->
            <StackPanel DockPanel.Dock="Top"
                        Orientation="Horizontal"
                        HorizontalAlignment="Center">
                <StackPanel.Resources>
                    <Style TargetType="{x:Type TextBlock}" BasedOn="{StaticResource TextBlockStyle}">
                        <Setter Property="FontSize" Value="8"/>
                        <Setter Property="Margin" Value="3,0"></Setter>
                    </Style>
                </StackPanel.Resources>
                <TextBlock Text="{Binding Employee.FullName, UpdateSourceTrigger=PropertyChanged, Mode=OneWay, FallbackValue=''}"/>
                <TextBlock Text="{Binding Date, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, StringFormat=dd-MMM-yyyy}"/>
            </StackPanel>

            <!-- Shift Selector -->
            <ComboBox DockPanel.Dock="Top"
                      Width="150"
                      Margin="5,0"
                      Style="{StaticResource MiniComboBoxStyle}"
                      ItemsSource="{Binding DepartmentRosterVM.Shifts, Mode=OneTime, UpdateSourceTrigger=PropertyChanged}"
                      SelectedValue="{Binding SelectedShift, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

            <!-- Shift Type and Attendance-->
            <StackPanel DockPanel.Dock="Top" 
                        Orientation="Horizontal" 
                        Width="150"
                        Margin="5,0">
                <!-- Shift Type Selector (Normal/AL/RDO/etc.) -->
                <ComboBox Style="{StaticResource MiniComboBoxStyle}"
                          Height="18"
                          Width="125"
                          SelectedValue="{Binding Type, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!-- Attendance -->
                <CheckBox Margin="0"
                          Padding="0"
                          ToolTip="At Work"
                          x:Name="IsAtWork"/>
            </StackPanel>

            <!-- Times -->
            <StackPanel DockPanel.Dock="Top"
                        Margin="0"
                        Orientation="Horizontal"
                        Width="150">
                <TextBox Height="18"
                         MinHeight="0"
                         FontSize="10"
                         Width="65"
                         Margin="0"/>
                <Label Content="to"
                       FontSize="10"
                       Margin="0"/>
                <TextBox Height="18"
                         MinHeight="0"
                         FontSize="10"
                         Width="65"
                         Margin="0"/>
            </StackPanel>

            <Frame></Frame>

        </DockPanel>
    </Border>
</UserControl>
