<UserControl x:Class="Pantheon.View.Controls.EmployeeControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:data="clr-namespace:Uranus.Staff.Model;assembly=Uranus"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">

    <UserControl.DataContext>
        <data:Employee x:Name="Employee"></data:Employee>
    </UserControl.DataContext>

    <StackPanel Orientation="Horizontal">
            <Image Source="{Binding Icon.FullPath, Mode=OneWay, FallbackValue=''}"/>
            <Label Content="{Binding}"/>
    </StackPanel>
</UserControl>
