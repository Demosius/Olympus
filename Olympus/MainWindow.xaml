<Window x:Class="Olympus.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Olympus" 
        xmlns:Control="clr-namespace:Olympus.Components"
        mc:Ignorable="d"
        Title="Olympus" WindowState="Maximized"
        Icon="./Resources/Images/Icons/olympus.png"
        MinWidth="400"
        MinHeight="300"
        Background="#022">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Control:Navbar x:Name="NavBar" Grid.Row="0" Grid.Column="0"/>
        <Frame  Margin="5,5,5,5" x:Name="OView" Grid.Row="1" Grid.Column="0" Height="Auto" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"
                BorderThickness="1" BorderBrush="SlateGray"/>
    </Grid>

    <Window.Resources>
        
    </Window.Resources>
</Window>
