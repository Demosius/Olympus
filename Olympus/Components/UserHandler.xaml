<UserControl x:Class="Olympus.Components.UserHandler"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Olympus.Components"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*" MaxWidth="100"/>
            <ColumnDefinition Width="*" MaxWidth="100"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0">Who are you, and what are you doing here?</Label>

        <Button Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" Height="Auto">Sign In</Button>
        <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="2" Height="Auto">Register</Button>
        
    </Grid>
</UserControl>
